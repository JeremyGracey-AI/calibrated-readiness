[pytest]
testpaths = tests
addopts = -q
# Keep pytest's cache off networked/overlay mounts to avoid slow or recursive
# temp cleanups in some sandboxed filesystems.
cache_dir = /tmp/.pytest_cache_calibrated_readiness
